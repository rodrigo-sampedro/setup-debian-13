# roles/preflight/templates/backup_manifest.j2
# Backup Manifest
# Generated by Ansible on {{ ansible_date_time.iso8601 }}

╔═══════════════════════════════════════════════════════════════╗
║                    CONFIGURATION BACKUP                       ║
╚═══════════════════════════════════════════════════════════════╝

Backup Date: {{ ansible_date_time.iso8601 }}
Backup Directory: {{ backup_dir }}
Hostname: {{ ansible_hostname }}
IP Address: {{ ansible_default_ipv4.address }}
Operating System: {{ ansible_distribution }} {{ ansible_distribution_version }}

════════════════════════════════════════════════════════════════
BACKED UP FILES
════════════════════════════════════════════════════════════════

{% for file in backup_files %}
{% if file is file %}
  ✓ {{ file }}
{% else %}
  ✗ {{ file }} (not found at backup time)
{% endif %}
{% endfor %}

════════════════════════════════════════════════════════════════
RESTORE INSTRUCTIONS
════════════════════════════════════════════════════════════════

To restore a configuration file:

1. Stop the affected service:
   systemctl stop <service_name>

2. Restore the file:
   cp {{ backup_dir }}/<filename> /original/path/

3. Restart the service:
   systemctl restart <service_name>

════════════════════════════════════════════════════════════════
IMPORTANT NOTES
════════════════════════════════════════════════════════════════

- This backup was created BEFORE the Ansible playbook made changes
- Keep this backup until you verify the system is working correctly
- Backups are stored in timestamped directories for multiple runs
- To revert all changes, use the files in this backup directory

════════════════════════════════════════════════════════════════
